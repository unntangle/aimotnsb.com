// Superseded: the printed membership form now downloads automatically when the
// application on /membership/apply is submitted. Kept as a stub so any stray
// import fails loudly rather than silently rendering a second download gate.
export {};
